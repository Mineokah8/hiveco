PORT=5000
MONGO_URI=mongodb://localhost:27017/hive
JWT_SECRET=your_jwt_secret
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_email_password
CLIENT_URL=http://localhost:3000
CLOUD_NAME=your_cloud_name
CLOUD_API_KEY=your_api_key
CLOUD_API_SECRET=your_api_secret
# Cloudinary configuration for image uploads
# This configuration is used to set up Cloudinary for handling image uploads in the Hive application.
# It includes the cloud name, API key, and API secret required for authentication with Cloudinary
# These values should be set in the environment variables for security and flexibility.
# The Cloudinary instance can be used to upload images, manage media assets, and perform other media-related tasks.
# This is essential for features like profile picture uploads, image sharing, and media management within the application.
# The module exports the configured Cloudinary instance for use in other parts of the application.
# It allows developers to easily integrate image upload functionality without needing to manage the configuration in multiple places.
# The Cloudinary configuration is crucial for enabling image uploads and media management in the Hive application.
# It ensures that images can be uploaded, stored, and retrieved efficiently, enhancing the user experience.
# The configuration is typically stored in environment variables to keep sensitive information secure and to allow for
# easy changes without modifying the codebase.
# The Cloudinary instance can be used throughout the application to handle image uploads, transformations, and
# other media-related tasks, making it a powerful tool for managing images in web applications.
# This configuration is essential for enabling image uploads and media management in the Hive application.
# It ensures that images can be uploaded, stored, and retrieved efficiently, enhancing the user experience.
# The configuration is typically stored in environment variables to keep sensitive information secure and to allow for
# easy changes without modifying the codebase.
# The Cloudinary instance can be used throughout the application to handle image uploads, transformations, and
# other media-related tasks, making it a powerful tool for managing images in web applications.
# This configuration is essential for enabling image uploads and media management in the Hive application.
# It ensures that images can be uploaded, stored, and retrieved efficiently, enhancing the user experience.
# The configuration is typically stored in environment variables to keep sensitive information secure and to allow for
# easy changes without modifying the codebase.
# The Cloudinary instance can be used throughout the application to handle image uploads, transformations, and
# other media-related tasks, making it a powerful tool for managing images in web applications.
# This configuration is essential for enabling image uploads and media management in the Hive application.
# It ensures that images can be uploaded, stored, and retrieved efficiently, enhancing the user experience.
# The configuration is typically stored in environment variables to keep sensitive information secure and to allow for
# easy changes without modifying the codebase.      
CLOUDINARY_URL=cloudinary://your_api_key:your_api_secret@your_cloud_name
REDIS_URL=redis://localhost:6379
REDIS_PORT=6379
REDIS_HOST=localhost
REDIS_PASSWORD=your_redis_password
REDIS_DB=0
REDIS_TLS=false
REDIS_USERNAME=default
REDIS_SASL_ENABLED=false
REDIS_SASL_MECHANISM=PLAIN
REDIS_SASL_PASSWORD=your_redis_sasl_password
REDIS_SASL_USERNAME=your_redis_sasl_username    


ADMIN_EMAIL=mineokah8@gmail.com
